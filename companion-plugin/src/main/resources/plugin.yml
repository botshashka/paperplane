name: PaperPlane
main: dev.paperplane.companion.CompanionPlugin
version: ${version}
# Must match minimum of Versions.SUPPORTED_API_VERSIONS in cli module
api-version: '1.18'
description: PaperPlane dev companion - in-game build status, save coordination, and plugin reload
load: POSTWORLD
